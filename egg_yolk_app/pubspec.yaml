name: egg_yolk_app
description: Mobile Application for Predicting Egg Yolk Color Score from Photographs
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  image_picker: ^1.0.7
  google_fonts: ^6.1.0
  flutter_spinkit: ^5.2.0
  path_provider: ^2.1.2
  image: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/model_weights.json
